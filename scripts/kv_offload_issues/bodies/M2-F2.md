**Summary:** The correctness protocol over the directory: a process-shared directory lock, a per-slot read/write pin (read = reload, write = evict), a per-slot generation counter for reuse/ABA detection, and a publish-on-DMA-completion gate. A stale or mid-write slot must degrade to a **cache miss, never torn bytes**.

### What

- Process-shared directory lock; per-slot rwlock-style pin; per-slot generation counter; publish-on-completion gate.

### Atomicity / critical-section contract (RFC §4.4)

The upper layer (the Python connector) drives a KV transfer as a **sequence** of directory ops — e.g. `claim` → device↔host DMA → `publish`, or `lookup` → pin → reload. This issue must state, per RFC §4.4, exactly which of those steps a single acquire of the directory lock covers and which the **caller composes** across separate calls:

- **Atomic within one lock hold:** each individual op (`lookup`, `claim`, `publish`, `evict`) is internally atomic against the shared directory — a concurrent process never observes a half-updated directory or slot state.
- **Caller-composed (not one lock):** the `claim` → DMA → `publish` span is **not** held under a single directory lock for the DMA's duration (the DMA is long and must not block other instances). Correctness across that span comes from the **pin + generation + publish gate**, not from holding the lock — so a slot mid-write is a **miss until `publish`**, and generation reuse between a caller's `lookup` and its DMA is caught by the generation check.
- The lock is held only for the short directory mutations, **never across a DMA**; blocking to acquire it is bounded and does not deadlock with a peer holding a pin.

This contract is what M2-T1 exposes to Python and what defines the GIL-release policy at the binding (see M2-T1).

### Unit / stress tests — full data-race matrix (full coverage on the racing cases)

Each case runs under concurrent multi-process load and asserts either correct data or a clean miss — never a torn read:

- **Reader vs. evictor:** reader reloads a slot while another instance evicts + re-DMAs it → reader sees pre-evict bytes (pin held) or a miss, never a mixture.
- **ABA / generation reuse:** slot evicted and re-claimed for a *different* hash between a reader's `lookup` and its DMA → generation check fails → miss.
- **Publish-gate visibility:** reader `lookup`s a slot mid-write (claimed, DMA not complete, not yet published) → miss (mapping invisible until `publish`).
- **Concurrent claim of the same hash:** two instances `claim` the same absent hash at once → exactly one slot allocated, the other observes the winner (no double-allocation).
- **Concurrent claim under exhaustion:** claims race with the pool full → eviction picks a victim safely (victim's pin respected), no slot handed out twice.
- **Writer vs. writer:** two instances offload *different* hashes concurrently → no cross-contamination; both publish correctly.
- **Reader during re-DMA of the same hash:** hash re-offloaded while a reader holds a read pin → reader completes on the old generation or misses; writer waits or picks another slot.
- **Sustained stress:** run the matrix for a sustained duration under N processes → **zero torn reads, zero double-allocations**.
- **Multi-op sequence under the GIL:** the matrix is also driven from Python threads through the M2-T1 bindings, so the `claim`→DMA→`publish` / `lookup`→pin→reload sequences race across *both* processes and Python threads. A binding that blocks on the directory lock must not stall unrelated Python threads (validates the GIL-release policy defined in M2-T1); the caller-composed span still yields a clean miss, never torn bytes.

### Acceptance

- [ ] All matrix cases pass.
- [ ] Sustained N-process stress reports zero torn reads and zero double-allocations.
- [ ] The atomicity / critical-section contract (which steps one lock hold covers vs. what the caller composes across calls) is documented per RFC §4.4, and the lock is verified **never held across a DMA**.
- [ ] Driven from concurrent Python threads through the M2-T1 bindings, a binding blocked on the directory lock does not stall unrelated Python threads, and the multi-op sequence still degrades to a miss, never torn bytes.

**Blocked by:** M2-F1 (__M2F1__)

_Part of Milestone 2 (__M2__)._
